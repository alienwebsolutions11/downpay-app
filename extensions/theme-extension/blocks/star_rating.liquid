<style>
  .options_purchase_radio_btn {
    display: block;
    margin-bottom: 6px;
  }
</style>

<script>
  window.productData = {{ product | json }};
</script>

<fieldset data-selling-plan-wrapper>
  <legend>Deposit only due at checkout</legend>

  {% assign first_plan = product.selling_plan_groups.first.selling_plans.first %}
  {% if first_plan %}
    <label class="options_purchase_radio_btn">
      <input type="radio"
             name="selling_plan_option"
             value="{{ first_plan.id }}"
             data-selling-plan>
      Deposit
    </label>
  {% endif %}

  <label class="options_purchase_radio_btn">
    <input type="radio"
           name="selling_plan_option"
           value=""
           data-selling-plan
           checked>
    Pay full
  </label>
</fieldset>

<script>
(function() {
  const productData = {{ product | json }};

  // Get product form dynamically
  function getForm() {
    return document.querySelector('form[action*="/cart/add"]');
  }

  // Get selected selling plan
  function getSelectedSellingPlan() {
    const checked = document.querySelector('[data-selling-plan]:checked');
    return checked ? checked.value : '';
  }

  // Universal fetch patch to inject selling_plan
  function patchFetch() {
    const originalFetch = window.fetch;
    window.fetch = async function(url, options) {
      if (typeof url === 'string' && url.includes('/cart/add') && options) {
        try {
          // JSON body
          if (options.headers?.['Content-Type']?.includes('application/json')) {
            const body = JSON.parse(options.body);
            const sp = getSelectedSellingPlan();
            if (sp) body.selling_plan = sp;
            options.body = JSON.stringify(body);
          } 
          // FormData fallback
          else if (options.body instanceof FormData) {
            const sp = getSelectedSellingPlan();
            if (sp) options.body.set('selling_plan', sp);
          }
        } catch(e) { console.error('Selling plan patch error', e); }
      }
      return originalFetch.apply(this, arguments);
    }
  }

  // Update all visible prices dynamically
  function updatePrices() {
    const form = getForm();
    if (!form) return;

    const variantId = form.querySelector('[name="id"]')?.value;
    if (!variantId) return;

    const variant = productData.variants.find(v => v.id == variantId);
    if (!variant) return;

    const depositInputs = document.querySelectorAll('[data-selling-plan-wrapper] [data-type="deposit"]');
    const fullPriceInputs = document.querySelectorAll('[data-selling-plan-wrapper] [data-type="full"]');

    depositInputs.forEach(el => {
      const val = parseFloat(el.dataset.value || 50);
      const amt = (variant.price * val / 100 / 100).toFixed(2);
      el.textContent = `${val}% deposit ($${amt})`;
    });

    fullPriceInputs.forEach(el => {
      el.textContent = '$' + (variant.price / 100).toFixed(2);
    });
  }

  // Watch for variant changes dynamically
  function watchVariantChange() {
    let lastVariant;
    setInterval(() => {
      const variantId = getForm()?.querySelector('[name="id"]')?.value;
      if (variantId && variantId !== lastVariant) {
        lastVariant = variantId;
        updatePrices();
      }
    }, 300);
  }

  function init() {
    patchFetch();
    updatePrices();
    watchVariantChange();
  }

  document.addEventListener('DOMContentLoaded', init);
  document.addEventListener('shopify:section:load', init);
})();
</script>

{% schema %}
{
  "name": "Deposit Option",
  "target": "section",
  "enabled_on": { "templates": ["product"] },
  "settings": []
}
{% endschema %}